# Core orchestration
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# LLM backend (Groq)
langchain-groq>=0.2.0
groq>=0.11.0

# Embeddings + vector store (local/free)
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
chromadb>=0.5.0

# Web search fallback (needed for the "corrective" retrieval-grading step)
langchain-tavily>=0.1.0
tavily-python>=0.5.0

# Document loading / parsing
pypdf>=4.0.0
beautifulsoup4>=4.12.0
unstructured>=0.15.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.0
tiktoken>=0.7.0

# LangGraph Postgres checkpointing
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.2.0
cryptography>=43.0.0  # required for EncryptedSerializer